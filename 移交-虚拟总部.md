# 墨境虚拟总部 — 项目移交

> 交给「总管的助手」对话框

## 当前状态

**AgentFleet 像素办公室已跑在**：`http://localhost:5176/static/office/`

后端API：`http://localhost:8802`（server.js + bot-runner.js）

## 项目路径

| 用途 | 路径 |
|------|------|
| AgentFleet前端（正在用的） | `D:\agents-office-frontend`（junction→`D:\建网站\参考项目\agents-office\frontend`） |
| 原版参考源码 | `D:\建网站\参考项目\agents-office\` |
| 另一参考项目（MIT许可） | `D:\建网站\参考项目\clawd-office\` |
| 我们自己的后端 | `D:\建网站\mojing-hq\server.js` |
| 文档中心 | `D:\建网站\mojing-docs\` |

## 技术栈

- 前端：AgentFleet（React + Phaser 3 + Vite + TypeScript），热更新已就绪
- 后端：Express 5 + Socket.IO 4，端口8802
- API：`/api/agents`（51个Agent）、`/api/assign`（分配任务）、`/api/v1/health`、`/api/v1/office/agent-registry`、`/api/v1/office/chat/stream`

## 已完成的改动

1. 标题 → "墨境"
2. Agent列表 → 从我们8802 API读取
3. 群聊消息 → POST到我们的/api/assign
4. Vite代理 → 指向localhost:8802（不是原来的8000）
5. 已添加3个AgentFleet兼容路由到server.js
6. 缺失组件已补（ScenarioTemplatePanel、DashboardView）

## 目标

把AgentFleet像素办公室彻底变成墨境集团的虚拟办公界面——能看到角色走动、聊天、干活。

## 启动命令

```bash
# 1. 后端
start "" "C:/Program Files/nodejs/node.exe" "D:/建网站/mojing-hq/server.js"
start "" "C:/Program Files/nodejs/node.exe" "D:/建网站/mojing-hq/bot-runner.js"

# 2. 前端
cd D:/agents-office-frontend
npx vite --port 5176 --host
```

⚠️ 注意：`D:\agents-office-frontend` 是 junction 软链接，sandbox 写不了——要通过 Python bash 或手动改 `D:\建网站\参考项目\agents-office\frontend\` 下的原文件。
