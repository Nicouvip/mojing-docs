# 一、已完成清单

## 1. 编辑器功能
| 功能 | 核心文件 |
|------|---------|
| TipTap 富文本编辑器 | `src/components/writing-editor.tsx` |
| 工具栏（加粗/斜体/下划线/标题/引用/列表/分隔线/对齐/撤销） | 同上 |
| 多色高亮标记 | 同上 |
| 实时字数统计 + 进度条 + 目标设定 | 同上 |
| 大纲自动提取（h1/h2/h3 → 侧边目录） | 同上 |
| 选中文字插入标签（主角/反派/配角） | 同上 |

## 2. 页面布局
| 功能 | 核心文件 |
|------|---------|
| 三栏布局（左侧栏 + 编辑区 + 右侧面板） | `src/app/editor/[id]/page.tsx` |
| 左侧栏折叠/展开（拉环按钮） | 同上 |
| 右侧图标工具栏折叠/切换（📋🔍📑👤💡🤖） | 同上 |
| 顶部导航栏（返回/项目名/保存/完成本章/专注模式） | 同上 |
| 专注模式（一键隐藏两侧） | 同上 |
| 毛玻璃效果（backdrop-blur） | 同上 + `globals.css` |
| 自定义半透明滚动条 | `globals.css` |
| 首页（作品列表 + 新建/删除作品） | `src/app/page.tsx` |

## 3. 左侧栏功能
| 功能 | 核心文件 |
|------|---------|
| 章节列表（按卷分组、搜索过滤） | `src/app/editor/[id]/page.tsx` |
| 新建章/新建卷 | 同上 |
| 全部/草稿筛选 | 同上 |
| 章节删除（移入回收站） | 同上 |
| 回收站弹窗（恢复/彻底删除） | 同上 |
| `···` 菜单（回收站/导入/导出/排序/全部章节） | 同上 |

## 4. AI 功能
| 功能 | API 路由 | 前端 UI |
|------|----------|---------|
| AI 续写 | `src/app/api/ai/continue/route.ts` | 🤖 面板 |
| AI 润色 | `src/app/api/ai/polish/route.ts` | 🤖 面板 |
| AI 扩写 | `src/app/api/ai/expand/route.ts` | 🤖 面板 |
| 脑洞喷射（含题材选择） | `src/app/api/ai/brainstorm/route.ts` | 💡 弹窗 |

## 5. 合规检测
| 功能 | 核心文件 |
|------|---------|
| A 类递进判断句检测 | `src/lib/compliance.ts` |
| B 类禁用词检测（同段≥3次触发） | 同上 |
| C 类连接词计数 | 同上 |
| 动作后解释语句检测 | 同上 |
| 身体密度计算（calcBodyDensity） | 同上 |
| 55字生死线检测（check55CharLine） | 同上 |
| 合规检测图标徽章（右侧栏显示违规数） | `src/app/editor/[id]/page.tsx` |
| 章末自检报告弹窗（6项检测 + 1-5分评分） | 同上 |

## 6. 数据存储
| 功能 | 核心文件 |
|------|---------|
| 内存 + localStorage 双模式持久化 | `src/lib/store.ts` |
| 作品 CRUD（create/delete/getProjects） | 同上 |
| 章节 CRUD（含软删除 + 回收站） | 同上 |
| 30秒自动保存 | `src/app/editor/[id]/page.tsx` |
| 章节切换保存（localStorage 记录上次章节） | 同上 |
| 类型定义（Project/Chapter/TechniqueRecord/Foreshadowing/SelfCheckReport） | `src/lib/types.ts` |

## 7. 导出与主题
| 功能 | 核心文件 |
|------|---------|
| TXT 单章导出 | `src/app/editor/[id]/page.tsx` |
| 4套主题切换（☀️默认/🌅暖光/🌙暗夜/❄️冷光） | 同上 + `globals.css` |
| 小说专用排版（思源宋体/首行缩进2em/行距2） | `globals.css` |
