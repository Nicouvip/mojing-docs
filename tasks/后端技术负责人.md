【强制技能 — test-driven-development · executing-plans · systematic-debugging · verification-before-completion】

- [ ] 完成Supabase数据库迁移
- [ ] 实现NextAuth真实认证

────────────────────────

T-P0-Auth：Supabase Auth 正式接入

当前 login/register 仍是 Mock 登录。工作区已有 Supabase 适配层代码（auth-store.ts + supabase.ts），需要：
1. 确认 Supabase 环境变量已配置（NEXT_PUBLIC_SUPABASE_URL、SUPABASE_SERVICE_ROLE_KEY）
2. 确认 auth-store.ts 的 Supabase Auth 适配层编译通过
3. 测试 login/register API 路由能正常调用 Supabase
4. 验证 .env.local 中的 Supabase 凭据有效性

参考：D:\建网站\mojing-docs\站内功能替代分析.md（第1-2节）
涉及文件：src/lib/db/auth-store.ts, src/lib/db/supabase.ts, src/app/api/auth/login/route.ts, src/app/api/auth/register/route.ts

验证：npx tsc --noEmit + 手动测试登录/注册流程

T-P1-API：修复灵感爆裂/书名炼金术API风险

基于提示词专家验证报告（output/提示词-API接口验证.md）：
1. R1 — 书名解析脆弱：Alchemy 靠 split('\n')，AI偏离格式就返回空。改用正则提取或要求AI输出JSON
2. R2 — Inspire mode返回不实
3. R3 — 无请求体校验：非法JSON直接throw 500

涉及文件：src/app/api/ai/alchemy/route.ts, src/app/api/ai/inspire/route.ts
验证：npx tsc --noEmit + curl测试

────────────────────────
输出文件：output/后端-SupabaseAuth+Alchemy修复.md
