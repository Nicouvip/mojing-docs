【强制技能 — executing-plans + test-driven-development】
────────────────────────

Sprint 补漏 P0：真实认证 + 数据库

两件事（按顺序）：
1. **数据库迁移**：localStorage → Supabase
   - 创建 Supabase 项目，获取 URL + anon key
   - 写入 .env.local（NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY）
   - src/lib/store.ts → supabase.from('projects').select() 替换 localStorage
   - 建表：projects / chapters / characters / outlines / materials

2. **用户认证**：Mock → NextAuth
   - /login + /register 接 src/auth.ts Google Provider
   - 删掉 auth-store.ts mock
   - 导航栏登录态接 NextAuth session

验证：换浏览器 → 登录 → 数据还在（不是 localStorage）
