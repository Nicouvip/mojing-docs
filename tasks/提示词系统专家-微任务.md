【强制技能 — executing-plans + chinese-documentation】
────────────────────────

**微任务：提示词模板 Token 成本核算与压缩**

1. 对 `src/lib/prompts/` 下每个模板计算：Prompt Token 数 × 模型单价（DeepSeek V4 Flash）× 预估月调用量 = 月成本
2. 标记成本占比 Top-3 的模板，给出压缩方案（删冗余指令 / 合并重复约束 / 精简少样本示例）
3. 压缩后重新核算，目标：总 Token 量减少 ≥ 30%
4. 输出到 `output/提示词-Token成本与压缩方案.md`

验收标准：压缩后输出质量不低于原版（人工评分 4/5 以上），月成本降低 ≥ 30%。
