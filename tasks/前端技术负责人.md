【强制技能 — frontend-design】
────────────────────────

Sprint 补漏：AI 端点+超时+卷管理

1. AI 超时保护（推迟3次了！）
   - 6 个 AI 端点 fetch() 加 AbortController + 30s 超时
   - 超时返回 504，前端显示"生成超时，请重试"

2. 垃圾桶自动清理
   - store.ts 加 `deletedAt` 字段
   - 定时器每天检查，超过 7 天自动彻底删除

3. 卷管理（重命名/删除卷）
   - 编辑器内加卷操作 UI

4. 导入 TXT
   - 菜单"导入"→ 选文件 → 解析 → 新建项目

验证：pnpm build 零错误
